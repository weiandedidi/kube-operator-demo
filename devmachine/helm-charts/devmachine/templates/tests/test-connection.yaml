apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devmachine.fullname" . }}-test-connection"
  labels:
    {{- include "devmachine.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devmachine.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
