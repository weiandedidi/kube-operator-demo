apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: devmachines.ide.example.com
spec:
  group: ide.example.com
  names:
    kind: DevMachine
    listKind: DevMachineList
    plural: devmachines
    singular: devmachine
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DevMachine is the Schema for the devmachines API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of DevMachine
            type: object
            required: # 定义一些需要强制传输的字段
              - cpu
              - memory
              - image
            properties:  # 增加这些字段的校验
              cpu:
                type: integer
                minimum: 1
                maximum: 128
                description: "Number of vCPU cores"
              memory:
                type: string
                pattern: '^[0-9]+(Gi|Mi)$'
                description: "Memory size (e.g., 32Gi)"
              gpu:
                type: integer
                minimum: 0
                default: 0
                description: "Number of GPU devices"
              image:
                type: string
                description: "Container image for the dev machine"
            x-kubernetes-preserve-unknown-fields: true
          status:
            type: object
            properties:
              phase:
                type: string
                enum:
                  - Pending
                  - Creating
                  - Running
                  - Stopping
                  - Stopped
                  - Failed
                description: "Current phase of the dev machine"
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
    subresources:
      status: {}
